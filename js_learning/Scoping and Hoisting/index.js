if (true) {
    let p = 1;
    console.log(p);
}
/*if console is outside the block the outcome result will be an error
 but if a "var" keyword is used then the console can either be outside or inside the block:
 */
console.log(y);
var y = 5;
/*your expected outcome will be "Undefined" because the console is above the declearation 
 but if the console is under the declearation then you can have an outcome of "5"
 this can only happen when using the "var" keyword:
*/

