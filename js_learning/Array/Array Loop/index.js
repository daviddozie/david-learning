var evenNumbers = [2,4,6,8,10];
/*the moment the value of "n" becomes the
level of array the statement will case to br true:
*/ 
for (let n = 0; n < evenNumbers.length; n++) {
    console.log(evenNumbers[n]);
}